/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['liquimoly.ru', ],
        domains: ['w7.pngwing.com'],
        domains: ["lemarc.ru"],
      },
};

export default nextConfig;
